import <%= Component %> from './base-<%= component %>';
import { <%= Component %>Props, Base<%= Component %>Props } from './<%= component %>.type';

export default (props: <%= Component %>Props) => {
  const {} = props;
  
  return <<%= Component %>></<%= Component %>>;
};
