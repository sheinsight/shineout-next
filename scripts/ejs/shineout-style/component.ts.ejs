// import token from '@sheinx/theme';
import { JsStyles } from '../jss-style';

export type <%= Component %>Class =
  | 'wrapper'
  | 'wrapperDisabled';

const <%= component %>Style: JsStyles<<%= Component %>Class> = {
  wrapper: {
    display: 'block',
  },
  wrapperDisabled: {},
};

export default <%= component %>Style;
