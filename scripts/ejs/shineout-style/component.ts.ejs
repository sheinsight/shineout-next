// import token from '@sheinx/theme';
import { JsStyles } from '../jss-style';

export type <%= Component %>Classes =
  {
    wrapper: string;
  };
export type <%= Component %>ClassType = keyof <%= Component %>Classes;

const <%= component %>Style: JsStyles<<%= Component %>ClassType> = {
  wrapper: {
    display: 'block',
  },
};

export default <%= component %>Style;
