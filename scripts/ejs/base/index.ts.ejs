export { default, default as <%= component.charAt(0).toUpperCase() + component.slice(1) %> } from './<%= component %>';
export type { <%= component.charAt(0).toUpperCase() + component.slice(1) %>Props } from './<%= component %>.type';
