/**
* cn - 基本用法
*    --
* en - Basic
*    --
 */
import React from 'react';
import { <%= Component %> } from '@sheinx/base';
import { use<%= Component %>Style } from '@sheinx/shineout-style';

const jssStyle = {
  <%= component %>: use<%= Component %>Style
}

export default () => {
  return (
    <div>
      <<%= Component %> jssStyle={jssStyle} />
    </div>
  );
};
