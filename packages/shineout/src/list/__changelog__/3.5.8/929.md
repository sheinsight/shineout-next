修复 `List` 设置 `size` 属性后选择行的 `Checkbox` 不跟随尺寸的问题

#### 示例
```jsx
import { List } from 'shineout';

const data = [
  { id: 1, name: 'Item 1' },
  { id: 2, name: 'Item 2' },
  { id: 3, name: 'Item 3' },
];

export default () => {
  return (
    <>
      <h3>Small Size</h3>
      <List
        size="small"
        data={data}
        keygen="id"
        format="id"
        renderItem={(d) => d.name}
        value={[1]}
      />
      
      <h3>Default Size</h3>
      <List
        data={data}
        keygen="id"
        format="id"
        renderItem={(d) => d.name}
        value={[2]}
      />
      
      <h3>Large Size</h3>
      <List
        size="large"
        data={data}
        keygen="id"
        format="id"
        renderItem={(d) => d.name}
        value={[3]}
      />
    </>
  );
};
```