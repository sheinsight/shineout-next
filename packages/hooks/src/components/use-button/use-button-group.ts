// import React, { Children, cloneElement } from 'react';
// import { ButtonGroupProps } from './use-button-group.type';
// import { BaseButtonProps } from './use-button.type';
//
const useButtonGroup = () => {
  //   const { size, type, outline, children } = props;
  //   const getRootProps = () => {};
  //   const getChildren = () => {
  //     return Children.toArray(children).map((child: React.ReactElement) =>
  //       cloneElement(child, { size, outline, type: type ? type : child.props.type }),
  //     );
  //   };
};

export default useButtonGroup;
